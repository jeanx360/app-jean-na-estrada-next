# ALTERAÇÕES — JNE APP 1.8.1

- A versão exibida no menu deixou de ser fixa.
- Menu e `/api/health` agora usam a versão do `package.json`.
- `package.json` e `package-lock.json` foram atualizados para 1.8.1.
- Cache da PWA atualizado para `jne-app-v1.8.1`.