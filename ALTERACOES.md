# Alterações 1.3.1

- O perfil é lido antes do recálculo automático de VIP.
- Contas `admin` não executam a rotina destinada a `member` e `vip`.
- A função SQL de recálculo possui proteção adicional no próprio `UPDATE`.
- Corrigida a função de aprovação de assinatura direta.
- Cache PWA atualizado para 1.3.1.
