# Testes — JNE App 2.1.1

Validar:

- branch e versões `2.1.1`;
- Service Worker `jne-app-v2.1.1`;
- ausência das chaves antigas do Google no código ativo;
- presença das APIs abertas em `api.heigit.org`;
- chave protegida no servidor;
- autocomplete retornando coordenadas;
- geocodificação reversa da localização atual;
- rota GeoJSON com distância, duração e geometria;
- mapa com atribuição ao OpenStreetMap;
- fallback manual sem chave;
- links externos para Google Maps e Waze;
- TypeScript;
- build de produção;
- health na versão `2.1.1`;
- ausência de resíduos do pacote.
